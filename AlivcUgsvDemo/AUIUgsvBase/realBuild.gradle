apply plugin: 'com.android.library'
apply from: '../script_config.gradle'

android {
    compileSdkVersion androidCompileSdkVersion
    buildToolsVersion androidBuildToolsVersion

    defaultConfig {
        minSdkVersion androidMinSdkVersion
        targetSdkVersion androidTargetSdkVersion
        consumerProguardFiles 'lib-proguard-rules.pro'

    }
    lintOptions {
        abortOnError false
    }
}

dependencies {
    implementation fileTree(include: ['*.jar','*.aar'], dir: 'libs')
    implementation externalAndroidXAppcompat
    implementation externalAndroidXRecyclerView
    implementation externalAndroidXConstraintlayout
    implementation project(':AliyunVideoCommon')
    implementation project(':AUIMedia')
    implementation externalAlivcConan
    implementation externalGSON
    implementation externalAlivcCore
    implementation externalAlivcFFmpeg

    if (buildProductType == SVIDEO_PRO || buildProductType == ALL||buildProductType == LITTLEVIDEO){
        api externalSvideopro
    }else if (buildProductType == SVIDEO_STANDARD){
        api externalSvideostandard
    }else if (buildProductType == SVIDEO_BASE){
        api externalSvideosnap
    }
}
